import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.notipush.zoolife',
  appName: 'AplicacionMovilZooLife',
  webDir: 'www'
};

export default config;
